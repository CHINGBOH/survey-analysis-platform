<!-- AUTO-GENERATED by scripts/docs — DO NOT EDIT BY HAND.
     Regenerate with `make docs` (or `python3 scripts/docs/gen_all.py`).
     Source of truth: regex scan of 01-clean/**/*.R, 02-analyze/**/*.R, 03-integrate/**/*.R, 04-report/**/*.R
     Generated: 2026-05-23 03:23:54  ·  git: 124def6
-->

# R API 参考

由正则扫描提取（无需 R 运行时）。文档块识别规则: 函数定义上方连续的 `#` 或 `#'` 注释。


## `01-clean/`

### `01-clean/clean_survey1.R`

> !/usr/bin/env Rscript 01-clean/clean_survey1.R 调查一 清洗编码：文本→数值 输入: data/raw/问卷数据_完整版_209条.xlsx 输出: data/cleaned/survey1.rds

*(无函数定义；可能是脚本式 R)*

### `01-clean/clean_survey2.R`

> !/usr/bin/env Rscript 01-clean/clean_survey2.R 调查二 清洗编码：文本→数值 输入: data/raw/问卷数据_精简版_206条.xlsx 输出: data/cleaned/survey2.rds

*(无函数定义；可能是脚本式 R)*


## `02-analyze/`

### `02-analyze/anova.R`

> !/usr/bin/env Rscript 02-analyze/anova.R — SPSS 等价单因素 ANOVA (generic) plan.json:   {"anova": {"specs": [{"dv": "extra_spend", "group": "age_group"}, ...]}} 无 plan 时自动:数值列 × 多分类列(3-8 levels) 输出 output/results/anova_<sid>.rds:   $summaries        rbind(每个 spec 一行)   $group_means      命名 list(spec_id →

依赖: `DBI`, `RSQLite`

- `read_plan()`
- `load_wide(survey_id)`
- `auto_detect(df)`
- `run_for_survey(survey_id)`

### `02-analyze/anova_legacy.R`

> !/usr/bin/env Rscript

依赖: `car`

*(无函数定义；可能是脚本式 R)*

### `02-analyze/cluster.R`

> !/usr/bin/env Rscript 02-analyze/cluster.R — SPSS 等价 KMeans / 层次聚类 / 判别 (generic)

依赖: `DBI`, `RSQLite`

- `read_plan()`
- `load_wide(survey_id)`
- `auto_numeric_vars(df)`
- `run_for_survey(survey_id)`

### `02-analyze/cluster_legacy.R`

> !/usr/bin/env Rscript

依赖: `MASS`

*(无函数定义；可能是脚本式 R)*

### `02-analyze/correlation.R`

> !/usr/bin/env Rscript 02-analyze/correlation.R — 相关分析（SQLite）

*(无函数定义；可能是脚本式 R)*

### `02-analyze/crosstabs.R`

> !/usr/bin/env Rscript 02-analyze/crosstabs.R — SPSS 等价 Crosstabs (generic) plan.json:   {"crosstabs": {"pairs": [["gender","used_voucher"], ["age_group","status"]]}} 无 plan 时自动:所有低基数分类列两两配对(最多 12 对) 输出 output/results/crosstabs_<sid>.rds:   命名 list: <row>_<col> → list(crosstab, expected, std_residual

依赖: `DBI`, `RSQLite`

- `read_plan()`
- `load_wide(survey_id)`
- `auto_detect_cat(df)`
- `run_for_survey(survey_id)`

### `02-analyze/crosstabs_legacy.R`

> !/usr/bin/env Rscript 02-analyze/crosstabs.R — 交叉表 + 卡方（SQLite）

*(无函数定义；可能是脚本式 R)*

### `02-analyze/descriptives.R`

> !/usr/bin/env Rscript 02-analyze/descriptives.R — SPSS 等价描述统计(generic) 用法:   Rscript 02-analyze/descriptives.R                  → 跑 survey1+2,自动选变量   Rscript 02-analyze/descriptives.R survey1          → 只跑 survey1 自动行为(无 plan.json 时):   - 数值列 → desc_table + normality   - 分类列(unique <= 20) → freq_tab

依赖: `DBI`, `RSQLite`

- `read_plan()` — ── 1. 读 plan.json(如有) ────────────────────────────────────────
- `load_wide(survey_id)` — ── 2. 加载一个 survey 的宽表 ─────────────────────────────────────
- `auto_classify_vars(df, plan)` — ── 3. 自动变量分类 ──────────────────────────────────────────────

### `02-analyze/descriptives_legacy.R`

> !/usr/bin/env Rscript 02-analyze/descriptives.R — SQLite 两表合并版本

依赖: `DBI`, `RSQLite`, `psych`

*(无函数定义；可能是脚本式 R)*

### `02-analyze/factor_analysis.R`

> !/usr/bin/env Rscript 02-analyze/factor_analysis.R — SPSS 等价 PCA / EFA (generic)

依赖: `DBI`, `RSQLite`

- `read_plan()`
- `load_wide(survey_id)`
- `auto_likert_items(df)`
- `run_for_survey(survey_id)`

### `02-analyze/factor_analysis_legacy.R`

> !/usr/bin/env Rscript 02-analyze/factor_analysis.R — 直接从宽表读

依赖: `DBI`, `RSQLite`, `psych`

*(无函数定义；可能是脚本式 R)*

### `02-analyze/mediation.R`

> !/usr/bin/env Rscript

依赖: `boot`

*(无函数定义；可能是脚本式 R)*

### `02-analyze/moderation.R`

> !/usr/bin/env Rscript

*(无函数定义；可能是脚本式 R)*

### `02-analyze/power_bootstrap.R`

> !/usr/bin/env Rscript

依赖: `boot`

*(无函数定义；可能是脚本式 R)*

### `02-analyze/rds_to_json.R`

> !/usr/bin/env Rscript 02-analyze/rds_to_json.R — 把任意 RDS 转 JSON,供 Python 解读侧消费 用法: Rscript 02-analyze/rds_to_json.R <rds_path> [max_rows] 限制 data.frame 行数,避免 LLM 上下文爆炸; list 中嵌套结构原样保留

依赖: `jsonlite`

- `trim(x)` — 递归裁剪 data.frame 行数,保留全部列

### `02-analyze/regression.R`

> !/usr/bin/env Rscript 02-analyze/regression.R — SPSS 等价回归分析 (generic) plan.json:   {"regression": {       "linear":     [{"response": "extra_spend", "predictors": ["age_num","income_num","gender_bin"]}],       "hierarchical": [{"response": "y", "blocks": [["x1","x2"], ["x3"], ["x4","x5"]]}],       "

依赖: `DBI`, `RSQLite`

- `read_plan()`
- `load_wide(survey_id)`
- `auto_detect(df)`
- `run_for_survey(survey_id)`

### `02-analyze/regression_legacy.R`

> !/usr/bin/env Rscript

依赖: `car`, `lmtest`, `pROC`

*(无函数定义；可能是脚本式 R)*

### `02-analyze/reliability.R`

> !/usr/bin/env Rscript 02-analyze/reliability.R — SPSS 等价 Cronbach α + 分半 + 项目分析 (generic)

依赖: `DBI`, `RSQLite`

- `read_plan()`
- `load_wide(survey_id)`
- `auto_likert_scales(df)`
- `run_for_survey(survey_id)`

### `02-analyze/reliability_legacy.R`

> !/usr/bin/env Rscript 02-analyze/reliability.R — 直接从 respondents 宽表读 Likert

依赖: `DBI`, `RSQLite`, `psych`

*(无函数定义；可能是脚本式 R)*

### `02-analyze/render_charts.R`

> !/usr/bin/env Rscript 02-analyze/render_charts.R — 把模块 RDS 自动渲染为图表 PNG 用法: Rscript 02-analyze/render_charts.R <module> <survey_id>   e.g. Rscript 02-analyze/render_charts.R descriptives survey1 输出:   output/charts/<module>_<sid>/*.png   output/charts/<module>_<sid>/manifest.json — 每张图的 path + title 

依赖: `jsonlite`

- `add_chart(name, title, type, p, w = 6.5, h = 4.5)`

### `02-analyze/survey_specific.R`

> !/usr/bin/env Rscript 02-analyze/survey_specific.R — 问卷专用 (Likert / 缺失 / 异常 / 文本)

依赖: `DBI`, `RSQLite`

- `read_plan()`
- `load_wide(survey_id)`
- `auto_likert(df)`
- `auto_numeric(df)`
- `auto_text(df)`
- `run_for_survey(survey_id)`

### `02-analyze/ttest.R`

> !/usr/bin/env Rscript 02-analyze/ttest.R — SPSS 等价 t 检验 (generic) 优先读 output/results/analysis_plan.json:   {"ttest": {       "one_sample":   [{"var": "impact_num", "mu": 3}, ...],       "independent":  [{"dv": "extra_spend", "group": "gender"}, ...],       "paired":       [{"var1": "x", "var2": "y"}

依赖: `DBI`, `RSQLite`

- `read_plan()`
- `load_wide(survey_id)`
- `auto_detect(df)`
- `run_for_survey(survey_id)`

### `02-analyze/ttest_legacy.R`

> !/usr/bin/env Rscript 02-analyze/ttest.R — t检验（SQLite）

*(无函数定义；可能是脚本式 R)*


## `03-integrate/`

### `03-integrate/compile.R`

> !/usr/bin/env Rscript 03-integrate/compile.R — 收集结果并编译为统一数据结构 读取 output/results/plan.json（若存在）决定编译哪些 survey × 模块； 否则回退为"编译所有已存在的结果"。

依赖: `jsonlite`

*(无函数定义；可能是脚本式 R)*

### `03-integrate/read_result.R`

> !/usr/bin/env Rscript 03-integrate/read_result.R — 读取单个模块结果 .rds，输出 JSON 用法: Rscript read_result.R output/results/reliability_s1.rds

依赖: `jsonlite`

*(无函数定义；可能是脚本式 R)*


## `04-report/`
